import { Link } from "react-router-dom";

const Header = (props) => {
  return (
    <>
      <header>
        <nav>
          <p>Hello, Name</p>
          <button>Log Out</button>
        </nav>
      </header>
    </>
  );
};

export default Header;
